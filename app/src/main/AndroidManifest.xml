<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bakalarkapokus">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BakalarkaPokus">
        <activity
            android:name=".Aktivity.AddCalendarActivity"
            android:exported="false"
            android:label="@string/title_activity_add_calendar"
            android:theme="@style/Theme.BakalarkaPokus.NoActionBar" />
        <activity
            android:name=".Aktivity.SearchedActivity"
            android:exported="false"
            android:label="@string/title_activity_searched"
            android:theme="@style/Theme.BakalarkaPokus.NoActionBar" />
        <activity
            android:name=".Aktivity.SpizActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".Aktivity.ReceptActivity" />
        <activity
            android:name=".Aktivity.AddRecept"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Aktivity.AdvanceActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Aktivity.CalendarActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Aktivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>